function century(year) {
  // calculate century from a given year
  // parameter is just year, which should be a number
  // should return the century, also a number
  // this number can be calculated from Math.ceil(year / 100)

  let century = Math.ceil(year / 100);
  return century;
}

/*
  Ember's Feedback:
  Great job!
*/
