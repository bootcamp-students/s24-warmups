SELECT *
FROM students
WHERE IsActive == 1;

/*
    Ember's Feedback:
    - Because TRUE is stored as 1 and FALSE as 0,
        you can just write "WHERE IsActive;" and it will still work
*/
