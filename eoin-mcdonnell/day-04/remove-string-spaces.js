function noSpace(x) {
  return x.replaceAll(" ", "");
}

// find the spaces and replaceAll them with no space
// assign variable to x
// repalceAll " ", ""
// iterate over string for str.length
// i guess regex all the patterns
// iterate over the
// reverting back to the first thing i tried

/*
  Ember's Feedback:
  Pseudocode needs work
  Good job using replaceAll
  put old code in a notes.js file if you want to keep it for later.
*/
