SELECT ABS(number1) AS abs,
       LOG(64, number2) AS log
FROM decimals

/*
       Ember's Feedback:
       - Great job! :D
       - When you do write abs() for the column, it will actually
       use the function name for the column name. It will work the same
       without "AS abs"
*/
