function noSpace(x) {
    return x.replaceAll(" ", "");
  }
  
  
  /*-
  function noSpace(x)
  {
  let i = 0;
  if (i < x.length, i++)
  {
    let newString = x[i];
    if (newString = " ")
    {
      newString.replace(" ", "");
      console.log(newString);
      }
      console.log(newString);
      return newString;
    }
  }
  -*/
  //find the spaces and replaceAll them with no space
  //assign variable to x
  //repalceAll " ", ""
  //iterate over string for str.lenght
  //i guess regex all the paterns
  //iterate over the 
  //reverting back to the first thing i treid 