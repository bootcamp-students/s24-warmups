function getSum(a, b){
    let min = Math.min(a,b),
        max = Math.max(a,b)
    return(max - min +1) * (max + min) / 2
    //Good luck!
  }
  //while a is less than b incement a and then add it to the total. 
  //while a is less than b incement a and then add it to the total. 
  //need to find smaller and larger number. 
