function double(array) {
  let result = array.map((x) => x*2)
  return result
}

//use array.map to multiply each instance of the array by 2
// .map returns a new array after using a provided function on each index.
// so the function is x(index) times 2
