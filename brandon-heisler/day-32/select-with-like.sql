SELECT first_name,
    last_name
FROM names
WHERE first_name LIKE '______%';

/*
 i need to select 2 specific columns from the table.
 * is all
 so specifically ask for the last and first names column then specify
 the length we need.

 */

 /*
    Ember's Feedback:
    - Great work!
    - make all sql file names snake case
 */
