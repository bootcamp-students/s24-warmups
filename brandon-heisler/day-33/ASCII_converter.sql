SELECT id,
    ASCII(name) AS name,
    birthday,
    ASCII(race) AS race
FROM demographics;
-- just going to filter name through ascii function and raname it to name,
-- do the same with race

/*
    Ember's Feedback:
    - Great work :D
*/
