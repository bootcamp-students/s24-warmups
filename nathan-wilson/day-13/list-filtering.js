function filter_list(l) {
  return l.filter((e) => {
    return typeof e != 'string';
  });
}

// an array of non negative numbers and strings

// expecting to filter out the strings and return an array of numbers

// [1,2,'a','b']),[1,2], 'For input [1,2,"a","b"]'

// I could not filter out the correct types, I could use the wrong array method/s

// Take the array and filter out typeof strings and return the filtered array

// I can do this! I believe in myself
