# hardest part of this kata was remembering the formulas... math. and python syntax


def get_size(w, h, d):

    area = 2 * (w * h + h * d + w * d)
    volume = w * h * d

    return [area, volume]


"""
    Ember's Feedback:
    - Good job! :D
"""
