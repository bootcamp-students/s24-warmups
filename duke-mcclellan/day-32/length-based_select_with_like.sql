SELECT first_name,
    last_name
FROM names
WHERE first_name LIKE '______%'

/*
    Ember's Feedback:
    - Great job :D
    - LIKE takes a pattern and there are few other options
        in PostgreSQL you can even use with regular expressions
*/
