// Is Divisible

// two &'s mean 'and'; || means 'or'

// Old code:
function isDivisible(n, x, y) {
    Number.isInteger()
    if (true) { Number.isInteger((n / x), (n / y)); }

    else (false);
}

function isDivisible(n, x, y) {
    if (true) { (n / x), (n / y) = 'whole number';
    } else { (n / x) = decimal; (n / y) = decimal; }
}

// Volume of Cuboid
