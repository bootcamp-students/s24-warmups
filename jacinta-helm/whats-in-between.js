function between(a, b) {
    // your code here
    let rangeArray= [];
    for (i = a; i <= b; i++){
      rangeArray.push(i)
    }
    return rangeArray;
  }

//P create a function that has a range
//create an array
//have the index starting at the first/ lowest number (a)
//have index increase by one to creat the range≥ 
