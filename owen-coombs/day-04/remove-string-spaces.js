function noSpace(x) {
    return x.replaceAll(' ', '');
}

// return will be a string
// use replaceAll
// return with no space
// link for method (https://www.w3schools.com/jsref/jsref_string_replaceall.asp)

/*
    Ember's Feedback:
    - Great job!
    - So replace all will iterate over your string and search for the first parameter you give it.
        If it finds it, it will replace it with the 2nd parameter you give it and return to you the new
        string.
*/
