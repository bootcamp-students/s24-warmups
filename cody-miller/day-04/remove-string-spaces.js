function noSpace(x){
  let truncated = x.replaceAll(" ", "");
  return truncated;
}
// Have to target all inner spaces, which means trim methods wont work
// Replace all can do this. Replace " " with "".

/* Ember's Feedback:
  Great job!
*/
