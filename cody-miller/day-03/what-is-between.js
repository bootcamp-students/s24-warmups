function between(a, b) {
  let betwixt = [];
  console.log(betwixt);
  for (let i = a; i <= b; i++) {
    betwixt.push(i);
    console.log(betwixt);
  }
  return betwixt;
  console.log(betwixt);
}
//Be able to take two intergers
//a and b
//return an array of all intergers between them
//loop through
//create an [] with i
//Inclusive of params

/* function between(a, b) {
  let betwixt = [];
  for (let i = a, i <= b, i++) {
    betwixt = [i];
  }
} */

//Be able to take two intergers
//a and b
//return an array of all intergers between them
//loop through
//create an [] with i
//Inclusive of params
//<= or >=
