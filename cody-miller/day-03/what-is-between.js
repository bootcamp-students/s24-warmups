function between(a, b) {
  let betwixt = [];
  for (let i = a; i <= b; i++) {
    betwixt.push(i);
  }
  return betwixt;
}

// Be able to take two integers
// a and b
// return an array of all integers between them
// loop through
// create an [] with i
// Inclusive of params
// <= or >=

/* Ember's Feedback:
    Clean these submissions up before you push them, try to treat this like code you will write on the job.
    Good job!
    That is pretty much the best way to do it.
*/
