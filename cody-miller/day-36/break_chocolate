def break_chocolate(n, m):
    breaks = 0;
    if n * m > 1:
        breaks = n * m -1
    return breaks;

# function breakChocolate(n,m) {
#   let breaks = 0;
#   if ( n * m > 1) {
#     breaks = n * m - 1
#   }
#   return breaks;
# }